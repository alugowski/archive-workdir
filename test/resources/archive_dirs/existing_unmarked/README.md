Work dir has an unmarked directory that conflicts with a directory of the same name in the archive.

Expected behavior:
 * no change to archive when run normally
 * overwrite changes if marked manually or run with --mark-all
