Workdir has a new unmarked directory with the same name as an existing directory in Archive.

Do nothing to avoid potential data loss. User can mark manually.

Expected behavior:
 * `basic expects UNCHANGED_ARCHIVE`